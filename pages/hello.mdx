import Layout from '@/components/layout/Layout';
import Seo from '@/components/Seo';

# Hello, world

This is a test

<div className='btn btn-accent'>Hello, world!</div>

test with `inline` code

go to [test](/test) or go to [google](https://google.com)

```py
print("Hello, world!")
```

```jsx
const add = (x: int, y: int) => x + y;
```

```go
if (err != nil) {
  return nil, err
}
```

> Here is something somebody said which is important.

export default ({ children }) => (
  <Layout>
    <Seo templateTitle='test' />
    <main>
      <section className=''>
        <div className='layout prose lg:prose-lg'>{children}</div>
      </section>
    </main>
  </Layout>
);
